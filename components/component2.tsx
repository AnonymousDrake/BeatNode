import Image, { StaticImageData } from 'next/image';

// Recenty Played Display
const Component2 = ({}: {}) => {
  return (
    <div className="flex flex-col flex-1">
      <h1 className="text-white font-bold text-2xl">Recently Played</h1>
    </div>
  );
};

export default Component2;
