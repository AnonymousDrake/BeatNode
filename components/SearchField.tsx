const SearchField = () => (
  <input
    type="text"
    id="music-search"
    name="music-search"
    placeholder="search..."
    className="px-4 rounded-full hidden sm:flex"
  />
);

export default SearchField;
