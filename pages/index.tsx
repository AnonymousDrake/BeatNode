import type { NextPage } from 'next';
import { Header, Wrapper, Input } from '@components';

const Home: NextPage = () => (
  <Wrapper>
    <Header />
    <Input />
  </Wrapper>
);

export default Home;
